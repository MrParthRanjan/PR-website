{
  "name": "pr-website",
  "version": "1.0.0",
  "description": "Professional Website - Web Development Project",
  "main": "index.js",
  "scripts": {
    "start": "browser-sync start --server public --files \"public/**/*\" --port 8000 --host 0.0.0.0",
    "dev": "browser-sync start --server public --files \"public/**/*\" --port 8000 --host 0.0.0.0",
    "build": "echo 'Build script here'",
    "test": "echo 'Tests here'"
  },
  "keywords": ["website", "web", "development"],
  "author": "MrParthRanjan",
  "license": "MIT",
  "devDependencies": {
    "browser-sync": "^2.27.11",
    "http-server": "^14.1.1"
  }
}
